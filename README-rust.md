# Rust Integration

See https://github.com/mrcjkb/rustaceanvim

There is no need to configure any additional LSP tools. Ensure
that rust-analyzer is installed in the normal location `~/.cargo/bin/rust-analyzer`.

Check the nvim state in

```
ls -latr ~/.local/state/nvim/
```

